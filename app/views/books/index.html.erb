<h5><strong>New book</storng></h5>
<%= form_with model: @book, local:true do |f| %>
  <h6>Title</h6>
  <%= f.text_field :title %>

  <h6>Opinion</h6>
  <%= f.text_area :body %>
  <br>
  <%= f.submit 'Create Book' %>
<% end %>